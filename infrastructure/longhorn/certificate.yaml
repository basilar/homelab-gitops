apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-tls
  namespace: longhorn-system
spec:
  secretName: longhorn-tls
  duration: 8760h
  renewBefore: 720h
  issuerRef:
    name: basilar-ca
    kind: ClusterIssuer
  dnsNames:
    - longhorn.basilar.local
